\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{zenacl}[2/03/2022 Import and configure ACL]
\RequirePackage{kvoptions}

\DeclareStringOption[acl]{confpackage}
\ProcessKeyvalOptions*

\ifisfinal%
  \usepackage[nohyperref]{\zenacl@confpackage}
\else
  \usepackage[nohyperref,review]{\zenacl@confpackage}
\fi
% Standard package includes
\usepackage{times}
\usepackage{latexsym}

% for proper rendering and hyphenation of words containing Latin characters
% (including in bib files)
\usepackage[T1]{fontenc}

% This is not strictly necessary, and may be commented out,
% but it will improve the layout of the manuscript,
% and will typically save some space.
\usepackage{microtype}

% This is also not strictly necessary, and may be commented out.
% However, it will improve the aesthetics of text in
% the typewriter font.
\usepackage{inconsolata}

\newcommand{\zzaclinit}{
  % enable href
  \zzhrefinit%
  % use natlib cite commands
  \zzaddcommandsnatlib%
  % disable acro italics/hover over for conferences that don't use it like AAAI
  \zzacrodisablelinks\relax}

\newcommand{\zzaclend}[1]{%
\bibliography{#1}
\bibliographystyle{acl_natbib}}
